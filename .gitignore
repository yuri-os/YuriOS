# personal + derived — never committed (SPEC §11–§13)
.env
.venv/
.wvenv/          # legacy WSL environment from older installs
__pycache__/
*.egg-info/
.pytest_cache/
build/
dist/

# her mind — seeded locally, its own git repo, never nested in this one
vault/

# multi-character runtime data (registries, Vaults, traces, cards, and media)
data/

# personal debug data, like Build #1's corpus
traces/
corpus/
tool-logs/
models/

# the fetched Live2D runtime + rigs (proprietary/free-material — never committed;
# populate with `python scripts/fetch_live2d.py`, B2 §8.2's rule)
web/live2d/vendor/

# her photos — generated locally (SPEC §7.6), personal like the corpus
selfies/

# frontend build (Vite) + npm — regenerated by `cd web && npm ci && npm run build`
web/node_modules/
web/dist/

# the Windows desktop shell's electron.exe — installed by WINDOWS' node, per
# machine (desktop-shell/README.md); the shell itself is four small files
desktop-shell/node_modules/
