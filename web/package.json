{
  "name": "yurios-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "YuriOS sanctuary frontend — the three.js + three-vrm room, bundled by Vite so its libraries are npm-managed (security updates via `npm audit`) instead of vendored.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.5"
  },
  "dependencies": {
    "@pixiv/three-vrm": "^3.5.5",
    "@pixiv/three-vrm-animation": "^3.5.5",
    "three": "^0.185.1"
  }
}
