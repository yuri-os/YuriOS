"""The mind (SPEC §15–§24) — YuriOS's autonomy engine.

Build #4 built the body, the room, the hands, and one outbound bus; its idle
machine was a scripted stand-in for the thing this package actually is: an
**always-on cognitive tick loop** (SENSE → APPRAISE → DECIDE → ACT → REFLECT →
REGULATE) that holds the exact same strings — the `VrmController`, the ambient
speech seam, the guarded tools — and decides for itself, on its own clock, what
to do with them. Everything here runs on the injected clock and journals what
it does; the journal is the product, not a debug log.
"""
